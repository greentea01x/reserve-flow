# Consistency report — ReserveFlow v2 spec (managing-editor pass)

Inputs: `work/DECISIONS.md` (source of truth), `work/BRIEF.md`, `work/build/md/01…12`. Output: `work/build/md/00-overview.md` (new), edited 01–12 in place, assembled `work/build/SPEC-v2.md` (00→12, `---` separators).

## 1. Editorial decisions taken (DECISIONS wins; where silent, simplest consistent option)

Recorded in section 02 as D-29 / D-30 and amendments to D-01, D-02, D-16, D-22 so every section can cite a D-id.

| # | Topic | Decision applied everywhere | Why |
|---|---|---|---|
| 1 | Business hours per room vs global | **Global only**: table `business_hours` (7 rows, weekday PK), edited on Settings page via `PUT /admin/business-hours`; no `room_id`, no override, no `PUT /admin/rooms/:id/business-hours`, no `Room.business_hours/has_hours_override`; `GET /settings` and `GET /calendar` return one global array. Per-room hours = Phase 2 backlog. | DECISIONS §B line 14 ("business hours… in Settings"), R-22, the fixed 18-row grid, and "simplest consistent option"; §B line 24 "room hours" read as "hours that apply to rooms". Touched: 02 D-02, 06 DDL/SQL (availability, utilization), 07, 08, 09 T-016, 10 day-2, 11 A7/A10, 12. |
| 2 | Check-in endpoints | **One endpoint** `POST /bookings/:id/check-in` for self and admin; server derives `checkin_method` (owner/attendee → SELF, ADMIN not involved → ADMIN, window to `end_at`); Phase 1.1 QR uses `POST /check-in/rooms/:roomCode` (method QR). `POST /admin/bookings/:id/check-in` and `?force=true` removed. Non-participant → `403 FORBIDDEN`. | DECISIONS "no rotating tokens", 03 FR-016 "action เดียว 3 ทางเข้า", 01 V-01; fewer routes. Touched: 02 D-17/D-22, 03, 04 FL-05, 07 7.3.5/7.5, 08, 09 T-050, 10 S-13/TC-CHK-019. |
| 3 | Admin check-in window | Self `[start−15, start+15)`; **ADMIN until `end_at`** (observable when `auto_release_enabled=false`). | 03 + 06 + 07 agreed; DECISIONS gives only the self window. Added to D-16, BR-08, 12 glossary. |
| 4 | Auto-release deadline | `start_at + checkin_grace_minutes` only; `GREATEST(start_at, confirmed_at)` removed from 06 (T6, sweep) and 04 note. | Approve after `start_at` is blocked (03 L3, 06 T3, 07) so GREATEST was a no-op and contradicted 06's own explanation. (D-30d) |
| 5 | Set-password token | better-auth `verifications` table (no `password_tokens`); TTL **invite 7 days, admin reset / forgot 24 h**, single-use, "resend" button; endpoints `POST /auth/forgot`, `POST /auth/set-password`, `POST /admin/users/:id/{resend-invite,reset-password}`; UI `/set-password?token=`. | 06 owns storage; 07 owns routes; TTLs were 24 h (03/01), 60 min (07), 48 h/30 min (10) → picked 06's split as the practical one. (D-29) Touched: 01 V-02, 02 D-08, 03, 04, 07, 08, 09 T-013, 10 S-03/day-2, 12. |
| 6 | Idempotency | Key stored on `bookings` row (`UNIQUE(created_by, idempotency_key)`); replay → `200` + `Idempotent-Replayed: true`; 4xx not stored; **no `idempotency_keys` table**, no `IDEMPOTENCY_IN_PROGRESS/KEY_REUSED`; CSV import has no Idempotency-Key (upsert is idempotent). | 07 C-10 is the owner and simplest; 06 table/error rows, 04 paragraph, 05 wording, 09 T-030, 10 S-08/TC-IDEM-011 aligned. |
| 7 | `approval_actions` table | **Removed** from 06 (DDL, H1, T3, grants, migration list, ERD) and from 04/07/09 text; decisions live in `bookings.decided_by/at/reason/reason_code` + `audit_logs` (`BookingView.history[]`). | 01 §1.4 / R-27 said cut; DECISIONS never mentions it; duplicate of audit. |
| 8 | `reason_code` enum | `CONFLICT_LOST, ADMIN_REJECTED, OWNER_CANCELLED, ADMIN_CANCELLED, OWNER_DISABLED, NO_SHOW, EXPIRED` (07's `NO_DECISION` → `EXPIRED`). | 06 owns the CHECK. |
| 9 | Error codes | api.md names everywhere: `SLOT_UNAVAILABLE / VERSION_CONFLICT / INVALID_STATUS_TRANSITION / CHECKIN_WINDOW_CLOSED / NO_BOOKING_IN_WINDOW`; `PENDING_NOT_ALLOWED`, `CAPACITY_EXCEEDED`, `MANUAL_RELEASE` dropped. | 07 catalogue is the owner; `headcount` is informational (UI warns, never blocks — D-30c). |
| 10 | Settings keys | Canonical list = 06 §6.10: `slot_increment_minutes, min_duration_minutes, max_duration_minutes(null), buffer_minutes, max_advance_days, min_lead_minutes, checkin_open_before_minutes, checkin_grace_minutes, auto_release_enabled, reminder_minutes_before`. 07 settings JSON, 02 D-12, 03 §3.7, 04 FL-05 renamed; `approval_sla_minutes`, `product_name`, `reply_to_email` dropped from settings (APP_NAME constant, `MAIL_FROM/MAIL_REPLY_TO` env). | 06 owns the key list. |
| 11 | Privacy field | `is_private` boolean (column = JSON field); `privacy PUBLIC/PRIVATE` wording removed from 03/04/12. | 06/07 already used it. |
| 12 | Title length | 1–200 (06 DDL / 07); 03 and 10 S-09 updated from 120. | |
| 13 | AUTO_RELEASED recipients | owner + ADMIN only (no attendee .ics CANCEL) — 03 note, 04 4B, 06 sweep/state table aligned. | DECISIONS §B explicit. Flagged below as a functional gap to reconsider. (D-30b) |
| 14 | EXPIRED email | `booking.expired` → owner kept (03, 06, 08 templates, 09 T-041); total 10 template keys / 9 events. | DECISIONS silent; otherwise request dies silently. (D-30a) |
| 15 | Detail-only edits | No email for title/description/is_private/special_request/headcount; attendee diff → .ics REQUEST/CANCEL (only when CONFIRMED). 07 PATCH row and 06 edit row aligned to 03 L11. | Three different rules before. (D-30e) |
| 16 | ADMIN creates booking in MANUAL room → CONFIRMED directly + loser sweep | Stated in 02 D-01; already in 03 L2, 06, 07. | |
| 17 | Deactivated user | old cookie → `401 UNAUTHENTICATED` (session revoked); sign-in / surviving session → `403 ACCOUNT_DISABLED` (04, 07, 03, 09 aligned). | |
| 18 | Hard-delete user | only with zero history → `409 USER_HAS_HISTORY` (02 D-27, 03 §3.6, 07 U-03). Rooms are never deleted (`active=false`). | |
| 19 | Reschedule deadline | owner before `start_at`; cancel before `end_at` (02 D-14, 03 BR-06/07, 07). | |
| 20 | Notification retry | backoff in SQL, max 8 attempts (~1 h) → FAILED (06 owner); 03 NFR-5, 09 T-040, 01 R-23 aligned (were 3 / 5). | |
| 21 | Reminder for bookings created inside T−15 | sent on next sweep tick (06 rule) — 09 T-047 DoD flipped; dedupe = `(booking_id, template_key, recipient_email, dedupe_key=epoch(start_at))`. | |
| 22 | Sweep locking | `booking.sweep` does **not** take the advisory lock (only moves rows out of the live set; status-guarded); stated in 06 §6.7, 05 P-02, 08 (sweep = the one exception to `mutate()`). | |
| 23 | Dependency bot | Dependabot everywhere (05/12 said Renovate, 10 said Dependabot). | |
| 24 | IDs / numbering | 05 design principles renamed `D-0x` → `P-0x` (collided with 02's D-xx); 05 risks `R-0x` → `TR-0x` (collided with 01's R-xx; mapped to 09 RK-xx); 06 subsections `06.x` → `6.x`; `can.checkin` → `can.check_in`; `NFR-Concurrency…` → `NFR-1..6 (name)`; TC vocabulary = ops.md `TC-CON-001 … TC-OPS-026` (01 V-07/R-40/V-10 rows rewritten; TC-SET-015 added to 03 FR-014/RTM); "เช็คอิน" → "เช็กอิน"; package scope `@reserveflow/*`; `photo_url` (not `image_url`); `PUBLIC_BASE_URL` (not `APP_ORIGIN`); `DATABASE_URL_MIGRATE`; `infra/db/init/01-roles.sql`; compose = `compose.yml` (profiles `dev`/`tools`) + `compose.staging.yml`; workflows in `.github/workflows/`; k6 script `tests/load/calendar-p95.js`; race tests in `apps/api/test/race/`; `bookingService.mutate()` (not `withRoomLock()`). |
| 25 | Scope trims | `/admin/approvals/bulk` removed from 09 (07 has none); `POST /admin/bookings/:id/release` removed from 04/09; delivery webhook T-106 moved to Phase 2 backlog; `uuidv7()` noted as available but DDL uses `gen_random_uuid()`; version-pin table lives only in 12 §12.3 (05 cross-refs); rollback steps only in 10 (09 cross-refs); utilization definition only in 03 BR-13 + 06 §6.9 (07 cross-refs). |
| 26 | Cross-references | `DECISIONS §B/§C/§D/§E` replaced by section refs (03 §3.4 / 05 / 08 / 09) since DECISIONS.md is not part of the HTML; stale "หัวข้อ 02-E / 02 §D / 02 C / §jobs / §visibility" fixed. |
| 27 | Headings | Sub-headings made Thai-first with English in parentheses (03, 04, 06, 07, 08, 09, 10, 11, 12). |
| 28 | Routes added to 08 | web: `calendar.tsx` (E9), `set-password.tsx` (E0b), `forgot.tsx`; admin: `bookings.tsx` (A4), `bookings.$id.tsx` (A5); schema files reflect 06; templates list = 10 keys; `deploy.sh`, `scrub-staging.sql` listed. |
| 29 | Numbers | Aug 2569 denominator: 20 working days → 180 h/room, 540 h for 3 rooms (06 §6.9 now matches 01 R-16 and 11). FR-015 audit viewer = W5 (T-058), not W6. |

## 2. Remaining contradictions / things I could NOT fully resolve

1. **AUTO_RELEASED and attendees' calendars** — following DECISIONS (owner + admins only) leaves a stale event in attendees' calendars after a no-show. 03, 04 and 06 writers all independently wanted `.ics METHOD:CANCEL` to attendees. Cost is one extra recipient list in the sweep (same template machinery). Recommend the orchestrator amend DECISIONS; I left D-30b as "review after UAT".
2. **Admin reschedule reason** — 10 S-15 originally required a reason for admin reschedule/drag&drop, role change, approval_mode change, holiday; 07 `PATCH /bookings/:id` has no `reason`. I aligned S-15 to 07 (reason only for reject + admin cancel). If the business wants a reason on admin reschedule, 07 PATCH body + 06 audit need a field.
3. **Native `<input type=date>`** — 11 UX-03 uses native date input for quick search (browser may show ค.ศ./พ.ศ. inconsistently); 09 RK-09 and 01 V-06 warned against it. I aligned 09 to 11 (native only for quick search/filters; own month picker in E3). Worth a UX decision.
4. **Capacity of W1/W3/W4 (84 h vs 80 h)** — 09 states it with a slip list; not an editorial issue but still an open staffing question (dev C or move T-015/T-055/T-056).
5. **`GET /bookings/:id/ics` / `/attendees` → `403 FORBIDDEN_PRIVATE`** while the main resource returns a masked view — intentional per 07 C-15/C-16, but a reviewer may still see "403 vs 404 vs masked" as three regimes; 03 §3.6 now explains the rule in one sentence.
6. **`headcount` semantic** — informational only (D-30c). 03 FR-004 acceptance changed accordingly; if the business actually wants capacity enforcement it is a one-row addition to the 07 catalogue (`422 CAPACITY_EXCEEDED`).
7. **`GET /calendar.business_hours` shape** — now a single global array; 7.4.3 example updated, but 11's text for E3/E9 still says "cell สีตาม 11.2" without naming the feed shape (fine, but the HTML builder should not re-introduce per-room hours).
8. Section length: 06 (~70 KB) and 07 (~70 KB) are still above target; no content duplicated elsewhere, so I did not cut — trimming candidates remain the H1/T4 SQL comments in 06 and the 6 worked examples in 07.

## 3. Five questions for a peer reviewer to focus on

1. **Does every write path that can put a row into CONFIRMED run the loser sweep (H1) inside the same transaction?** Check 06 T1 (AUTO create incl. ADMIN-in-MANUAL), T3 (approve), T4 (admin reschedule / owner AUTO reschedule) against 03 L1/L3/L9 and 07 `rejected_conflicts` — and that constraint B would block the winner if any path forgot it.
2. **Is the one-endpoint check-in design airtight?** `POST /bookings/:id/check-in` derives `checkin_method` from the actor (owner/attendee → SELF, other ADMIN → ADMIN with window to `end_at`). Verify 03 FR-016/L12, 04 FL-05, 06 T6, 07 7.3.5/7.5 and 09 T-050 all say the same thing, and that an ADMIN who is also the owner gets SELF (not the extended window).
3. **Settings keys and numbers**: grep the assembled spec for `slot_increment_minutes … reminder_minutes_before`, 30-min / 60-min / 30-day / 15/15 / 7-day / 30-day / 5 failures / 8 retries — any stray old name (`advance_window_days`, `reminder_before_min`, `checkin_early_minutes`, `checkin.open_before_min`) or number (10 failures, 12 h, 60 min token) means a section I missed.
4. **Token flow**: 06 says better-auth `verifications`, 07 wraps `auth.api.resetPassword`; confirm better-auth 1.7 actually allows creating a reset token server-side for a user who has never set a password (invite) and different TTLs per purpose — if not, the `password_tokens` table the 07 writer wanted should come back (one migration) and D-29 updated.
5. **Phase labels and RTM status**: every FR/NFR row in 03 §3.8, every ticket in 09 and every TC in 10 uses exactly `MVP` / `Phase 1.1` (short "1.1") / `Phase 2`; NFR-4 (drag&drop) is labelled 1.1 while its board + `PATCH` endpoint are MVP — is that reading acceptable to the business given NFR-4 is an official PDF NFR?
